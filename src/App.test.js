import { render, screen } from '@testing-library/react';
import App from './App';

// test('render without errors', () => {
//   const { getByText } = render(<App />)
//   //render(<App />);
//   const title = getByText(/HOME de GIFS/i);
//   expect(title).toBeInTheDocument();
// });

test('asd', () => {
  const carlos = 10;

  expect(carlos).toEqual(10);
})